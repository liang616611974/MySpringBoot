<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <!-- 1.全局设置 -->
    <settings>
        <!-- 这个配置使全局的映射器启用或禁用缓存 -->
        <setting name="cacheEnabled" value="false"/>
        <!-- 允许JDBC支持生成的键。需要适合的驱动。如果设置为true则这个设置强制生成的键被使用，尽管一些驱动拒绝兼容但仍然有效（比如Derby） -->
        <setting name="useGeneratedKeys" value="false"/>
    </settings>

    <!-- 2.别名定义 -->
    <typeAliases>
        <package name="com.lf.admin.demo.model"/>
        <package name="com.lf.core.model"/>
        <!--
         <package name="com.lf.core.model"/>
         <package name="com.lf.admin.dev.model"/>
         -->
    </typeAliases>

<!--    &lt;!&ndash; mapping 文件路径配置 &ndash;&gt;
    <mappers>
        <mapper resource="classpath:/com/lf/**/mapper/*.xml" />
        &lt;!&ndash;
        <mapper resource="com/lf/admin/dev/dao" />
        <mapper resource="com/lf/core/dao" />
        &ndash;&gt;
    </mappers>-->


</configuration>
